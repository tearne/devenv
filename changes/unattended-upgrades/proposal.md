# Proposal: Unattended Upgrades
**Status: Note**

## Intent
The `unattended-upgrades` apt package should be installed and configured to automatically install all updates from pre-existing repositories as they become available.
